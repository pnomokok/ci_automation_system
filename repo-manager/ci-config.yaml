runtime: python
image: python:3.11-slim

steps:
  install:
    command: echo "No dependencies to install"
    timeout: 60
  build:
    command: echo "No build step"
    timeout: 60
  test:
    command: echo "1 passed in 0.1s"
    timeout: 60

branches:
  - main
  - develop
  - feature/repo-manager